<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.dromara.procurement.mapper.PmsPurchaseOrderMapper">

    <resultMap type="org.dromara.procurement.domain.vo.PmsPurchaseOrderVo" id="PmsPurchaseOrderResult">
        <id property="id" column="id"/>
        <result property="orderNo" column="order_no"/>
        <result property="title" column="title"/>
        <result property="requestId" column="request_id"/>
        <result property="requestCode" column="request_code"/>
        <result property="projectId" column="project_id"/>
        <result property="projectName" column="project_name"/>
        <result property="supplierId" column="supplier_id"/>
        <result property="supplierName" column="supplier_name"/>
        <result property="amount" column="amount"/>
        <result property="status" column="status"/>
        <result property="orderDate" column="order_date"/>
        <result property="deliveryDate" column="delivery_date"/>
        <result property="remark" column="remark"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectVoPageList" resultMap="PmsPurchaseOrderResult">
        SELECT
            o.*,
            p.project_name,
            s.supplier_name,
            r.request_code
        FROM pms_purchase_order o
        LEFT JOIN pms_project p ON o.project_id = p.id AND p.del_flag = '0'
        LEFT JOIN pms_supplier s ON o.supplier_id = s.id AND s.del_flag = '0'
        LEFT JOIN pms_procurement_request r ON o.request_id = r.id AND r.del_flag = '0'
        <where>
            AND o.del_flag = '0'
            <if test="bo.orderNo != null and bo.orderNo != ''">
                AND o.order_no LIKE CONCAT('%', #{bo.orderNo}, '%')
            </if>
            <if test="bo.title != null and bo.title != ''">
                AND o.title LIKE CONCAT('%', #{bo.title}, '%')
            </if>
            <if test="bo.requestId != null">
                AND o.request_id = #{bo.requestId}
            </if>
            <if test="bo.projectId != null">
                AND o.project_id = #{bo.projectId}
            </if>
            <if test="bo.supplierId != null">
                AND o.supplier_id = #{bo.supplierId}
            </if>
            <if test="bo.status != null and bo.status != ''">
                AND o.status = #{bo.status}
            </if>
            <if test="bo.params.beginCreateTime != null and bo.params.endCreateTime != null">
                AND o.create_time BETWEEN #{bo.params.beginCreateTime} AND #{bo.params.endCreateTime}
            </if>
        </where>
        ORDER BY o.create_time DESC
    </select>

</mapper>
